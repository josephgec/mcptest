"""Reusable scripted agents shipped with mcptest.

These are dumb, rule-based clients you can point pack tests at so that
`mcptest install-pack X && mcptest run` works end-to-end before you've
plugged in your real agent. Replace them with your own as soon as you've
seen the first green run.
"""
